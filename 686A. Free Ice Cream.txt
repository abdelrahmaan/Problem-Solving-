#include <bits/stdc++.h>
using namespace std;
long long n  , x , child  , num;
char  ch;
int main(){
    scanf("%lld%lld" , &n , &x);
    for(int i= 0; i < n; ++i){
        cin >> ch >> num;
        if( ch == '-')
            if(num > x )
              child ++;
              else
                x -= num;
        else
            x +=num;
    }
        printf("%lld %lld\n" , x , child);
            return 0;
}