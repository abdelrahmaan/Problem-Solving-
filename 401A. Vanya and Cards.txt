#include <bits/stdc++.h>
using namespace std;
const int N = 1e2+9 , OO = 1000000009;
int n , x  , cards , nCards;
int main() {
    scanf("%d%d" ,&n , &x );
    for(int i =0; i < n; ++i){
        scanf("%d" , &cards);
         nCards += cards;
    }
    printf("%d\n" , (abs(nCards)+ x-1) / x);
        return 0;
}