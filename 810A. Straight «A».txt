#include <bits/stdc++.h>
using namespace std;
const int N = 1e2+9 , OO = 1000000009;
int n , k , marks , sum , hackNum , addMarks;
int main() {
    scanf("%d%d" ,&n , &k);
    for(int i = 0; i <n; ++i){
        scanf("%d" , &marks);
        sum += marks;
    }
    while(1){
        if(round((double)sum / (n + addMarks) ) == k) break;
        sum += k;
        ++addMarks;
    }
    printf("%d\n" , addMarks);
        return 0;
}