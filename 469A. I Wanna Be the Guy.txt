#include <bits/stdc++.h>
using namespace std;
int maxLevels , level, x ,   ans;
set<int> st;
int main(){
    scanf("%d"  ,&maxLevels);
    for(int j = 0 ; j < 2; ++j){
    scanf("%d" , &level);
    for(int i = 0 ; i < level; ++i){
        scanf("%d" , &x);
        st.insert(x);
    }
      }
    if(st.size() == maxLevels)
        printf("I become the guy.\n");
    else
        printf("Oh, my keyboard!\n");
    return 0;
}