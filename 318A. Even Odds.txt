#include <bits/stdc++.h>
using namespace std;
long long n , f , di , ans;
int main(){
cin >> n >> f;
di = (n + 1) / 2;
if(f <= di)
    ans = 2 * f -1;
else
    ans = (f - di) * 2;
cout<< ans <<endl;
    return 0;
}