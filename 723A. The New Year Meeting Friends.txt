#include <bits/stdc++.h>
using namespace std;
int A[5] , x , y , z, sum;
string s;
int main(){
    for(int i = 0; i < 3; ++i)
        scanf("%d" , &A[i]);
        sort(A, A+3);
        reverse(A , A+3);
        sum += (A[0] - A[1]) + (A[1] - A[2]);
        printf("%d\n" , sum);
return 0;
}
