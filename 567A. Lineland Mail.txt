#include <bits/stdc++.h>
using namespace std;
int n  , A[100006] , minii[100006] , maxii[100006];
int main(){
    scanf("%d" , &n);
    for(int i = 0; i< n; ++i)
            scanf("%d" , &A[i]);
        for(int i = 0; i < n; ++i){
                if(i ==0){
                minii[i] = abs(A[i] - A[i+1]);
                maxii[i] = abs(A[i] - A[n-1]);
                }else if( i == n-1){
                    minii[i] = abs(A[i] - A[i-1]);
                    maxii[i] = abs(A[i] - A[0]);
                }else{
                    minii [i] = min(abs(A[i] - A[i+1])  , abs(A[i] - A[i-1]) );
                    maxii [i] = max(abs(A[i] - A[n-1]) , abs(A[i] - A[0]));
                }
        }
           for(int i = 0; i < n; ++i)
           printf("%d %d\n" , minii[i] , maxii[i]);

                         return 0;
}


____________________________Anothr Solution___________________________________
#include <bits/stdc++.h>
using namespace std;
    int n ,i,minimum,maximum , A[1000006];
int main(){
    scanf("%d",&n);
    for(i=0; i<n; i++)
        scanf("%d",&A[i]);

        for(i=0; i<n; i++){
            if(i == 0){
                minimum = A[i+1] - A[i];
                maximum = A[n-1] - A[i];
            }else if(i == (n-1)){
            minimum = A[n-1] - A[n-2];
            maximum = A[n-1] - A[0];
            }else{
            minimum = min((A[i+1] - A[i]) , A[i] - A[i-1]);
            maximum = max((A[n-1] - A[i]) , A[i] - A[0]);
            }
                    printf("%d %d\n" , minimum , maximum);

        }
    return 0;
}