#include<bits/stdc++.h>
using namespace std;
int  diff , ans;
string s;
char ch ='a';
int main ()
{
    cin >> s;
    for(int i = 0; i < s.length(); ++i){
        diff =abs(ch - s[i]);
        if(diff > 13)
            diff = 26 - diff;
        ans +=diff;
        ch = s[i];
    }
            printf("%d\n" , ans);
            return 0;
}